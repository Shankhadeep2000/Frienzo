<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sarkar.frienzo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Frienzo">
        <activity android:name=".Activities.SetProfileActivity"></activity>
        <activity android:name=".Activities.GroupChatActivity" />
        <activity android:name=".Activities.ChatActivity" />
        <activity android:name=".Activities.EmailSignInActivity" />
        <activity android:name=".Activities.OtpActivity" />
        <activity android:name=".Activities.PhoneSignInActivity" />
        <activity android:name=".Activities.EmailSignUpActivity" />
        <activity android:name=".Activities.PhoneSignUpActivity" />
        <activity android:name=".Activities.SignInActivity" />
        <activity android:name=".Activities.SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity" />
    </application>

</manifest>